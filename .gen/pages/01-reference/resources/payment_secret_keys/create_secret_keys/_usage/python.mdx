```python CreateSecretKeys.py
import belvo
from belvo.models import shared

s = belvo.Belvo(
    security=shared.Security(
        password="",
        username="",
    ),
)


res = s.payment_secret_keys.create_secret_keys()

if res.secret_keys is not None:
    # handle response
    pass
```
