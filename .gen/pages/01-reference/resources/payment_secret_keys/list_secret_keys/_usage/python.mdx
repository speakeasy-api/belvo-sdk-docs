```python ListSecretKeys.py
import belvo
from belvo.models import shared

s = belvo.Belvo(
    security=shared.Security(
        password="",
        username="",
    ),
)


res = s.payment_secret_keys.list_secret_keys()

if res.secret_keys_paginated_response is not None:
    # handle response
    pass
```
