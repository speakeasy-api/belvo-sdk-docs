```python CreateBankAccount.py
import belvo
from belvo.models import shared

s = belvo.Belvo(
    security=shared.Security(
        password="",
        username="",
    ),
)

req = shared.CreateBankAccountPse(
    shared.HolderBusinessPse(
        information=shared.HolderInformationBusinessPse(
            name='Trusty documentation services',
        ),
        type=shared.EnumBankAccountHolderTypePse.BUSINESS,
    ),
    metadata=shared.CreateBankAccountPseMetadata(),
    number='12343453245633',
    shared.ProvidersRequestPse(
        payments_way=shared.ProvidersRequestBodyPse(
            form_id=321,
            terminal_id=123,
        ),
    ),
)

res = s.bank_accounts.create_bank_account(req)

if res.create_bank_account_201_application_json_one_of is not None:
    # handle response
    pass
```
