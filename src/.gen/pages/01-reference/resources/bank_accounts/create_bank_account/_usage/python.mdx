```python CreateBankAccount.py
import belvo
from belvo.models import components

s = belvo.Belvo(
    security=components.Security(
        password="",
        username="",
    ),
)

req = components.CreateBankAccountPse(
    components.HolderBusinessPse(
        information=components.HolderInformationBusinessPse(
            name='Trusty documentation services',
        ),
        type=components.EnumBankAccountHolderTypePse.BUSINESS,
    ),
    metadata=components.CreateBankAccountPseMetadata(),
    number='12343453245633',
    components.ProvidersRequestPse(
        payments_way=components.ProvidersRequestBodyPse(
            form_id=321,
            terminal_id=123,
        ),
    ),
)

res = s.bank_accounts.create_bank_account(req)

if res.two_hundred_and_one_application_json_one_of is not None:
    # handle response
    pass
```
