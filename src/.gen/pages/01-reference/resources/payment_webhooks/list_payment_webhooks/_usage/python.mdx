```python ListPaymentWebhooks.py
import belvo
from belvo.models import components

s = belvo.Belvo(
    security=components.Security(
        password="",
        username="",
    ),
)


res = s.payment_webhooks.list_payment_webhooks()

if res.payments_webhooks_paginated_response is not None:
    # handle response
    pass
```
