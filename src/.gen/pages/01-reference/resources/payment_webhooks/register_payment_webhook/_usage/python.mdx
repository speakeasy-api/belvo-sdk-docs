```python RegisterPaymentWebhook.py
import belvo
from belvo.models import components

s = belvo.Belvo(
    security=components.Security(
        password="",
        username="",
    ),
)

req = components.PaymentWebhookRequest(
    auth_header='Authorization',
    auth_token='Bearer hv7alD71j0dzXf09iKfg7u',
    name='Your webhook name',
    url='https://mycompany.com/security/webhook',
)

res = s.payment_webhooks.register_payment_webhook(req)

if res.two_hundred_and_one_application_json_classes is not None:
    # handle response
    pass
```
